<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bendrieji stiliai -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/button_background</item>
        <item name="colorOnPrimary">@color/button_text</item>
        <item name="colorSecondary">@color/button_background</item>
        <item name="colorOnSecondary">@color/button_text</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- Ekrano tekstas -->
    <style name="TextDisplay" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">@color/text_display</item>
        <item name="android:textSize">30sp</item>
    </style>

    <!-- Mygtukų stiliai -->
    <style name="ButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:background">@color/button_background</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="ButtonPressedStyle">
        <item name="android:background">@color/button_pressed</item>
    </style>
</resources>
